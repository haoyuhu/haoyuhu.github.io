把作品集当成一个静态页面很容易，但那通常意味着内容会慢慢再次被写死回代码里。

我更想把它做成一个本地优先的内容操作系统：

- 页面只负责渲染已经整理好的 bundle。
- 简历、短文、长文都来自统一的 YAML 与 Markdown。
- CLI、Skill、Studio GUI 复用同一套 schema 与服务层。
- 发布之前必须通过校验、类型检查和页面回归。

这样每一次内容更新都不是“改页面”，而是“运行一次可靠的内容工作流”。
